[user]
	name = Giacomo Pati
	email = giacomo_pati@swissre.com
	; email = giacomo@pati.ch
[credential]
	helper = store
	; credentialStore = plaintext
[alias]
	st = status
	sync = fetch -p --all
	undo = checkout HEAD --
	conflicted = !nvim +Conflicted
[push]
	default = simple
[core]
	longpaths = true
[pull]
	rebase = true
